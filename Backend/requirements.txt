# ============================================================================
# Argos2 — Dependencias del Backend (Flask API + Visión Computacional)
# ============================================================================
# SIN versiones fijadas: pip resuelve la versión más reciente compatible,
# evitando roturas por incompatibilidad de versiones específicas.
#
# Las dependencias transitivas (Werkzeug, Jinja2, MarkupSafe, click,
# itsdangerous, blinker, colorama, typing_extensions, etc.) NO se listan
# aquí: pip las instala automáticamente según lo requieran Flask y
# Flask-Limiter. Fijarlas manualmente era la causa de roturas de instalación.
# ============================================================================

# ----------------------------------------------------------------------------
# Framework web y utilidades
# ----------------------------------------------------------------------------
Flask                  # Framework web (API REST)
flask-cors             # Cabeceras CORS para el frontend
Flask-Limiter          # Rate limiting (protección de endpoints)
python-dotenv          # Carga de variables de entorno desde .env

# ----------------------------------------------------------------------------
# Autenticación y seguridad
# ----------------------------------------------------------------------------
PyJWT                  # Generación y validación de tokens JWT
bcrypt                 # Hash de contraseñas

# ----------------------------------------------------------------------------
# Captura de vídeo y procesamiento de imágenes
# ----------------------------------------------------------------------------
opencv-python          # OpenCV (cv2): captura de vídeo y dibujado de overlays
numpy                  # Operaciones con arrays (frames, detecciones, métricas)
requests               # Cliente HTTP (sondeo de streams RTSP, comprobaciones)

# ----------------------------------------------------------------------------
# Visión computacional — Roboflow
# ----------------------------------------------------------------------------
# Opción 2 (Cloud): inferencia en la nube de Roboflow vía SDK.
inference-sdk

# Opción 3 (Local/Edge): inferencia local con el paquete `inference`.
# NOTA: es una dependencia PESADA (arrastra torch/onnxruntime, ~2 GB).
# Descomenta las líneas siguientes SOLO si vas a usar inferencia LOCAL:
# inference
# torch
# onnxruntime

# ----------------------------------------------------------------------------
# Pruebas
# ----------------------------------------------------------------------------
pytest                 # Framework de tests (necesario para Backend/tests/)
